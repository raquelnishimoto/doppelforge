{
  "name": "doppelforge",
  "version": "0.0.1",
  "private": true,
  "description": "Forge convincing doubles of your data from TypeScript types",
  "packageManager": "pnpm@11.15.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "workspaces": [
    "packages/*"
  ]
}